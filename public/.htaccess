Options -MultiViews
RewriteEngine On

# Serve existing files directly (assets, css, js, images)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# All other requests → index.html (React Router)
RewriteRule ^ /index.html [L]
